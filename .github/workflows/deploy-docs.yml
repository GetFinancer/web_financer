name: Deploy docs to release branch

on:
  push:
    branches:
      - main
    paths:
      - 'docs/**'

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout main branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configure git
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"

      - name: Prepare release branch (docs)
        run: |
          if git ls-remote --exit-code origin release; then
            git checkout -B release origin/release
          else
            git checkout --orphan release
            git rm -rf .
          fi

          # Alles löschen außer .git
          find . -mindepth 1 -maxdepth 1 \
            ! -name '.git' \
            -exec rm -rf {} +

          # Nur docs/ Inhalt aus main übernehmen
          git checkout main -- docs/
          cp -r docs/. .
          rm -rf docs

      - name: Commit and push to release
        run: |
          git add .
          git commit -m "Deploy docs from main (${GITHUB_SHA})" || echo "Nothing to commit"
          git push origin release --force
